export enum Pack {
    madagascar,
    costarica,
    indonesia,
    china
}

export enum Rarity {
    common,
    uncommon,
    endangered,
    rare
}

export enum Border {
    blue,
    yellow,
    orange,
    red,
    purple
}

export enum TrophicLevel {
    primary,
    secondary,
    tertiary,
    quaternary,
    quinary
}

export enum SpeciesType {
    plant,
    mammal,
    reptile,
    bird,
    amphibian,
    invertebrate,
    insect,
    fish
}